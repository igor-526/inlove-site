# Stand domain (documentation only; never a runtime fallback): inlove-stand.eqcms.ru
# Required absolute public backend API URL; browser requests are sent directly (no Next.js proxy).
NEXT_PUBLIC_API_BASE_URL=
# Required non-secret public build-time tenant selector sent as X-Equestrian-Service-Key; no fallback.
# Production CI validates presence without logging the configured value before image push/deploy.
NEXT_PUBLIC_EQUESTRIAN_SERVICE_KEY=
SENTRY_ENABLED=false
SENTRY_DSN=
SENTRY_ENVIRONMENT=
SENTRY_TRACES_SAMPLE_RATE=0
SENTRY_RELEASE=
